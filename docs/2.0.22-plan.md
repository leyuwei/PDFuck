# 2.0.22 implementation and verification

1. Separate fixed window headings from scrollable bodies, including Lab windows, settings, drawing, and modal dialogs. Verify scrollbar geometry on small windows and display scaling.
2. Replace the page-level three-retry loop with shared, cancellable recovery: classify service/network errors, back off, adapt incompatible parameters and output limits, validate/repair structured responses, split oversized automatic annotation inputs without dropping source blocks. Stop on unrecoverable credentials/configuration failures and on an elapsed recovery budget; surface the reason and next action.
3. Route annotation-list content double-click to the existing annotation dialog. Remove inline editors and per-row settings panels, move AI suggestions into the dialog, retain quick status actions.
4. Reuse a rich text editor for annotation bodies and replies, with localized format controls and platform shortcuts. Keep plain Contents for compatibility, store validated format ranges and standard rich contents in PDF, preserve styles through save/reopen and undo.
5. Add model/persistence and browser regressions; update affected existing UI tests. Run typecheck, unit and translation checks, source UI suite, Windows packaging and packaged-app regressions. Document actual outcomes and limitations.

Completed: all five steps. Verification: 401 unit tests and 56 release checks passed; see [2.0.22 validation](2.0.22-validation.md).
