# PDFuck Electron 重构计划

此文件是 TypeScript + React + Electron 重构的执行基线。每完成一个可运行、可验证的阶段，都同步更新 `PROGRESS.md`；旧 Python 版本只在新版本通过全部验收后删除。

## 1. 目标与不变项

- 界面继续只保留查看、编辑、批注、保存四个模块，沿用现有蓝灰色无边框风格。
- 保留连续滚动、单页、缩放、适合宽度、页码跳转和高清重渲染。
- 保留裁切、删页、格式化文字叠加。
- 保留高亮、便笺、替换、插入文字、删除线、下划线六类批注。
- 保留文字直接框选、页面右键快捷批注、已有批注选择/移动/双击编辑、高亮说明和批注列表行内编辑。
- 保留 PDF、JPG、PNG、EPS 保存导出、拖放打开、未保存保护和快捷键。
- Windows 先交付；主进程与渲染层不写 Windows 专用业务代码，为 macOS 打包预留配置。

## 2. 技术方案

- Electron：桌面窗口、原生文件对话框、拖放路径、菜单与安全文件写入。
- React + TypeScript + Vite：四模块 UI、工具状态、对话框和批注列表。
- PDF.js：PDF 解析、按缩放与设备像素比渲染、文字层和文字命中。
- pdf-lib：页面裁切/删除、文字写入、标准 PDF 批注字典、保存。
- Canvas：PNG/JPG 页面导出；EPS 使用兼容的栅格图像 EPS 编码，每页单独输出。
- Vitest：坐标、命名、批注模型和文档操作单元测试。
- Electron 实际窗口控制：启动成品程序、打开文件、操作主要控件并完成截图验收。

## 3. 安全与进程边界

- Renderer 禁用 Node.js 集成并开启 context isolation、sandbox。
- Preload 只暴露经过类型约束的文件和窗口操作，不透传任意 IPC。
- Main process 校验 PDF 输入路径、导出路径、格式和 IPC 来源。
- PDF 数据以 `Uint8Array` 在受控 IPC 中传递；写入采用同目录临时文件后替换。

## 4. 迁移阶段

1. 基线与留痕：建立 PLAN、迁移功能矩阵、版本和目录约定。
2. 工程骨架：完成 Electron/Vite/React/TypeScript、安全 preload、无边框窗口和开发/构建脚本。
3. 文档内核：完成打开、密码错误反馈、PDF.js/pdf-lib 双模型同步、脏状态、保存与另存为。
4. 查看器：完成连续/单页、高 DPI 重渲染、缩放、适宽、页码、拖放和文字层。
5. 编辑：完成裁切、删页和带字体/字号/颜色/粗斜体/对齐的文字叠加。
6. 批注：完成六类批注、右键菜单、文字框选、插入点吸附、页面命中/移动/双击编辑、批注列表及行内编辑。
7. 导出：完成 PDF、PNG、JPG、EPS，验证多页命名和 72-600 DPI。
8. 验收：执行类型检查、单元测试、端到端交互、PDF 保存后重开、页面 PNG 视觉检查和 Windows 安装包启动。
9. 清理：仅在第 8 阶段全部通过后删除 Python/PySide6/PyMuPDF/Pillow 代码、测试、脚本和旧构建产物，更新 README 与设计文档。

## 5. 完成定义

- `npm run typecheck`、`npm test`、`npm run build` 全部通过。
- 示例 PDF 可拖入并完成四模块核心流程；保存后的 PDF 可重新打开且修改仍存在。
- PNG/JPG/EPS 每页产物可识别，PDF 页面渲染截图清晰且界面无溢出。
- Windows 安装包或便携程序可在未安装 Node.js 的环境中启动。
- `PROGRESS.md` 无未完成迁移项，旧 Python 实现已删除且仓库入口全部指向 Electron。

## 6. 完成状态

- [x] 阶段 1：基线、功能矩阵与迁移边界。
- [x] 阶段 2：Electron + React + TypeScript + Vite 工程骨架。
- [x] 阶段 3：文档内核、脏状态、安全保存与另存为。
- [x] 阶段 4：连续/单页高清查看器、文字层、缩放与拖放。
- [x] 阶段 5：裁切、删页、格式化文字编辑。
- [x] 阶段 6：六类批注、页面直接交互和批注列表行内编辑。
- [x] 阶段 7：PDF、PNG、JPG、EPS 导出。
- [x] 阶段 8：类型、单元、构建、桌面交互、视觉与成品启动验收。
- [x] 阶段 9：旧 Python 实现和旧构建产物清理，文档入口切换。

## 7. v1.0.1 严重问题修复计划

- [x] 复现添加文字弹窗无法稳定输入的问题。
- [x] 在页面指针捕获释放后延迟聚焦文本框，隔离弹窗键盘事件。
- [x] 按 PDF.js 字体上升量重新计算文字顶部，修复把基线当作顶部造成的批注纵向错位。
- [x] 统一裁切框、页面旋转、显示坐标、PDF Rect 与 QuadPoints 的双向转换。
- [x] 批注增删改移动仅更新覆盖层，不再重新加载整份 PDF，消除闪白。
- [x] 批注提交后清除临时蓝色选区，避免与正式批注叠色。
- [x] 在界面、应用信息和 Windows 文件元数据中声明 `github@leyuwei` 版权。
- [x] 完成 v1.0.1 最终构建、视觉检查与成品启动验收。

## 8. v1.2.0 编辑效率升级计划

- [x] 为高亮、便笺、替换、插入、删除和下划线设计统一且可区分的矢量图标。
- [x] 在左侧工具、页面右键菜单和批注列表复用同一套图标。
- [x] 把新增文字改为带唯一编号、边界、内容、格式和外观流的 PDF FreeText 对象。
- [x] 编辑模块中支持单击选中文字对象、拖动改位、双击修改内容与完整格式。
- [x] 支持保存后重新打开并继续识别、编辑和移动由 PDFuck 创建的文字对象。
- [x] 新增页码多选视窗，支持当前页、奇数页、偶数页、清空和任意多页选择。
- [x] 批量删除按倒序一次提交，禁止删除全部页面并修复结构编辑后的页缓存。
- [x] 增加文字持久化/移动/编辑和非连续批量删页回归测试。
- [x] 完成实际界面、保存后 PDF 渲染、Windows 成品启动与交付检查。
